# Legacy cross-attention detector fragment used by older shell ablation scripts.

model:
  type: 'YOLODualModalOBB'
  num_classes: 5
  channels: [64, 128, 256, 512]
  fusion:
    type: 'DualStreamFusion'
  norm_type: 'GN'
  use_contrastive: True
