# Legacy RDM fusion fragment kept for optional ablation scripts.

model:
  type: 'YOLODualModalOBB'
  num_classes: 5
  channels: [64, 128, 256, 512]
  fusion:
    type: 'RDMFusion'
  norm_type: 'GN'
  use_contrastive: True
